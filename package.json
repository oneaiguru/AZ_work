{
  "name": "example-chrome-extension",
  "version": "1.0.0",
  "description": "Example Chrome extension that changes background color",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cd extension && zip -r ../dist/simple-extension.zip .",
    "test": "node test/test.js"
  }
}
